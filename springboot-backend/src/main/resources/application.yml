spring:
  datasource:
    url: 'jdbc:postgresql://localhost:49153/accounts_management_system'
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  error:
    include-message: always
  port: 8082